# Bet Tracker

GitHub Pages frontend for the Bet Tracker app. The frontend connects to the separate Supabase Bet-Tracker project for authentication and per-user cloud data.

Deploy `index.html` from the `main` branch with GitHub Pages.
